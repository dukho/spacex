<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.nomadworks.spacex.screens.home.HomeFragment"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_latestLaunchFragment"
            app:destination="@id/latestLaunchFragment" />
    </fragment>
    <fragment
        android:id="@+id/latestLaunchFragment"
        android:name="com.nomadworks.spacex.screens.latestlaunch.LatestLaunchFragment"
        android:label="Latest Launch"
        tools:layout="@layout/fragment_latest_launch" />
</navigation>